receivers:
  nop:

processors:
  chqenforcer:

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [chqenforcer]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [chqenforcer]
      exporters: [nop]
    logs:
      receivers: [nop]
      processors: [chqenforcer]
      exporters: [nop]
