receivers:
  nop:

exporters:
  chqstats:

processors:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [chqstats]
    metrics:
      receivers: [nop]
      processors: [nop]
      exporters: [chqstats]
    logs:
      receivers: [nop]
      processors: [nop]
      exporters: [chqstats]
