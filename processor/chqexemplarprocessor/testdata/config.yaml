receivers:
  nop:

processors:
  chqexemplars:
    reporting:
      interval: 100s

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [chqexemplars]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [chqexemplars]
      exporters: [nop]
    logs:
      receivers: [nop]
      processors: [chqexemplars]
      exporters: [nop]

extensions:
  nop:
