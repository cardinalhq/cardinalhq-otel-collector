chqstats/default-config:
chqstats/override-config:
  endpoint: http://localhost:8080
  timeout: 500ms
  compression: zstd
  headers:
    Alice: Bob
  timeboxes:
    logs:
      interval: 100s
    metrics:
      interval: 200s
    traces:
      interval: 300s
