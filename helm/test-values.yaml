vendors:
  datadog:
    enabled: false

collector:
  l1:
    additionalReceivers:
      - name: kafkametrics
        enabled: true
        logs: false
        metrics: true
        traces: false
        config:
          brokers:
            - kafka:9092
          topic: otel-metrics

chqs3:
  config:
    s3uploader:
      s3_bucket: my-special-bucket
