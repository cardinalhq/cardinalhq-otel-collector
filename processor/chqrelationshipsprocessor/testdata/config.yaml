receivers:
  nop:

processors:
  chqrelationships:
    reporting:
      interval: 600s

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [chqrelationships]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [chqrelationships]
      exporters: [nop]
    logs:
      receivers: [nop]
      processors: [chqrelationships]
      exporters: [nop]

extensions:
  nop:
