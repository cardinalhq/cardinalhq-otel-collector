receivers:
  nop:

processors:
  pitbull:
    statistics:
      endpoint: http://localhost:8080
      timeout: 500ms
      compression: zstd
      headers:
        Alice: Bob
      interval: 100s
      phase: presample
      vendor: alice
    configuration_extension: nop

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [pitbull]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [pitbull]
      exporters: [nop]
    logs:
      receivers: [nop]
      processors: [pitbull]
      exporters: [nop]

extensions:
  nop:
