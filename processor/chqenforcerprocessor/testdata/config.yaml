receivers:
  nop:

processors:
  chqenforcer:
    statistics:
      endpoint: http://localhost:8080
      timeout: 500ms
      compression: zstd
      headers:
        Alice: Bob
      interval: 100s
      phase: presample
      vendor: alice
    configuration_extension: nop

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers: [nop]
      processors: [chqenforcer]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [chqenforcer]
      exporters: [nop]
    logs:
      receivers: [nop]
      processors: [chqenforcer]
      exporters: [nop]

extensions:
  nop:
